import 'package:flutter/material.dart';

/// Paleta de colores de Aprendía — inspirada en artesanía mexicana
class AppColors {
  AppColors._();

  // Primarios — terracota
  static const Color primary = Color(0xFFB5451B);
  static const Color primaryLight = Color(0xFFD4694A);
  static const Color primaryDark = Color(0xFF8B3214);

  // Secundarios — marigold (flor de cempasúchil)
  static const Color secondary = Color(0xFFF5A623);
  static const Color secondaryLight = Color(0xFFF7BC5A);
  static const Color secondaryDark = Color(0xFFC4861C);

  // Acento — verde jade
  static const Color accent = Color(0xFF2E7D5E);
  static const Color accentLight = Color(0xFF4CAF88);
  static const Color accentDark = Color(0xFF1B5C43);

  // Neutros
  static const Color background = Color(0xFFFDF8F3);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceVariant = Color(0xFFF5EDE3);

  // Texto
  static const Color textPrimary = Color(0xFF1A1A1A);
  static const Color textSecondary = Color(0xFF6B5E52);
  static const Color textHint = Color(0xFF9E8E82);

  // Chat
  static const Color userBubble = Color(0xFFB5451B);
  static const Color tutorBubble = Color(0xFFEDE0D4);
  static const Color userBubbleText = Color(0xFFFFFFFF);
  static const Color tutorBubbleText = Color(0xFF1A1A1A);

  // Estados
  static const Color success = Color(0xFF2E7D5E);
  static const Color error = Color(0xFFD32F2F);
  static const Color warning = Color(0xFFF5A623);
}
